

fn main() {
    // addition
    let sum = 5 + 10;

    // subtraction
    let difference = 95.5 - 4.3;

    // multiplication
    let product = 4 * 30;

    // division
    let quotient = 56.7 / 32.2;
    let truncated = -5 / 3; // Results in -1

    // remainder
    let remainder = 43 % 5; 

    println!("{}",sum);

    get_volt()

    // Recusive formula for find the mean

    // alpha = (k-1) / k
    // x_bar[k] = (alpha * x_bar[k-1]) + ((1 - alpha)*x_bar[k])
}

fn get_volt(){
    let rand = 11.1111111212;
    let w = 4.0*rand; 
    let z = 14.4 + w;
    println!("{}", z);

} 


